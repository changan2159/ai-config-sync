interface:
  display_name: "Code Review"
  short_description: "Review code changes for concrete merge risks"
  default_prompt: "Use $code-review to review code changes for concrete bugs, regressions, missing tests, and verification gaps."

policy:
  allow_implicit_invocation: true
