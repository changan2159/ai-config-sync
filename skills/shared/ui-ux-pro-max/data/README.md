This skill is vendored as a self-contained repo-local package.

The original package referenced external `src/ui-ux-pro-max/...` paths that were not
present in this repository. Keep any future datasets inside this directory so the skill
remains portable across Codex, Claude Code, and OpenCode.
