Repository-local skills are grouped by ownership here.

Cross-client shared skills live under `skills/shared/`.
Client-specific repo skills live under `skills/<client>/`, for example `skills/codex/`.
Client-native system skills stay with their own CLI instead of being mirrored through `ai-config-sync`.
