Pi-specific repo skills live here.

Each direct child directory with a `SKILL.md` is synced only when the Pi target adds this directory through its own `skillRoots`.
This repository currently syncs this root automatically for the managed Pi target.
