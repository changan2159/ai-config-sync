- 2026-06-19: Moved shared MCP entrypoints to repo-local `tools/mcp/` wrappers, vendored repo-local MCP runtimes under `vendor/mcp/`, switched shared config to `${REPO_ROOT}`/`${HOME}` placeholders, limited shared skill sync to repo-local `skills/` only, and finished the Serena chain by vendoring `serena-agent` plus a repo-local launcher.
- 2026-06-19: Added repo-local MCP update entrypoints in `ai_config_sync.cli` plus shell wrappers under `scripts/mcp/` and `update-all-mcp.sh`; `serena-manager` stays manual/in-repo while `mcp-update-all` updates `serena-agent`, `codegraph`, and `node-repl-linux` with cross-component rollback.
- 2026-06-19: Added repo-local MCP preflight/toolchain preparation with `toolchain.lock.json` and `mcp-preflight`; runtime wrappers now fail fast and use pinned repo-local `vendor/toolchain` paths instead of invoking host PATH `uv` / `npm` / `node` during MCP startup.
