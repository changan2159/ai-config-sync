- 2026-06-19: Moved shared MCP entrypoints to repo-local `tools/mcp/` wrappers, vendored repo-local MCP runtimes under `vendor/mcp/`, switched shared config to `${REPO_ROOT}`/`${HOME}` placeholders, limited shared skill sync to repo-local `skills/` only, and finished the Serena chain by vendoring `serena-agent` plus a repo-local launcher.
- 2026-06-19: Added repo-local MCP update entrypoints in `ai_config_sync.cli` plus shell wrappers under `scripts/mcp/` and `update-all-mcp.sh`; `serena-manager` stays manual/in-repo while `mcp-update-all` updates `serena-agent`, `codegraph`, and `node-repl-linux` with cross-component rollback.
- 2026-06-19: Added repo-local MCP preflight/toolchain preparation with `toolchain.lock.json` and `mcp-preflight`; runtime wrappers now fail fast and use pinned repo-local `vendor/toolchain` paths instead of invoking host PATH `uv` / `npm` / `node` during MCP startup.
- 2026-06-19: Added managed OpenCode install/service flow in `ai-config-sync`: healthy releases install under `~/.local/share/ai-config-sync/opencode/releases/<version>`, `/home/admin101/.local/bin/opencode` probes `current` then `previous`, and the canonical boot path is now the system unit `opencode-web.service` while the old user unit `opencode-web-managed.service` stays disabled so a broken global `/usr/local` install no longer blocks CLI or Web.
- 2026-06-19: Added `scripts/claude/update-claude.sh`, upgraded the native Claude Code install from `2.1.178` to `2.1.183`, and removed the conflicting global npm `@anthropic-ai/claude-code` install plus its stale `/usr/local/bin/claude` symlink so Claude now uses only the native user-level updater path.
- 2026-06-23: Debugged `pi-web` OAuth provider loading on this host. Root cause was an upstream path-probe bug that misses the user-level `pi` install under `~/.local/lib/node_modules/@earendil-works/pi-coding-agent`; fixed the live environment with a compatibility symlink under `~/.npm-global/lib/node_modules/@earendil-works/pi-coding-agent`, and updated `ai-config-sync` so managed `pi-web` installs create that symlink and inject `PI_AI_OAUTH_INDEX` into the generated `pi-web.service` unit.
